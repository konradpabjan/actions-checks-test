
name: Manual Check Run

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Manually create a check run with some output
        uses: actions/github-script@v4
        with:
          script: |
            github.checks.create({
                name: 'Custom output',
                head_sha: '${{github.sha}}',
                owner: '${{github.repository_owner}}',
                repo: 'actions-checks-test',
                status: 'completed',
                conclusion: 'success',
                output: {
                  title: 'Custom Mardkown Content',
                  summary: '#### Hello World! :rocket: \r\n- You can easily manually create check runs using GITHUB_TOKEN and something like actions/github-script@v4  \r\n- Full markdown is supported and we've seen users do some really cool things ',
                },
                started_at: new Date()
            });

